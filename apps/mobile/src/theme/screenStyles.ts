import { appColors } from './uiStyles';

export const screenStyles = {
  container: {
    flex: 1,
    backgroundColor: appColors.bg,
  },
  content: {
    padding: 16,
    gap: 12,
  },
  card: {
    borderRadius: 12,
    borderWidth: 1,
    borderColor: appColors.border,
    backgroundColor: appColors.canvas,
    padding: 12,
    gap: 10,
  },
  listCard: {
    borderRadius: 12,
    borderWidth: 1,
    borderColor: appColors.border,
    backgroundColor: appColors.canvas,
  },
};
